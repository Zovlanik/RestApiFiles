CREATE TABLE IF NOT EXISTS `restapi`.`users` (
                                   `id` INT NOT NULL AUTO_INCREMENT,
                                   `username` VARCHAR(100) NULL,
                                   `id_account` INT NULL,
                                   PRIMARY KEY (`id`));
